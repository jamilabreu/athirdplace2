- content_for :javascript do
	= javascript_include_tag 'isotope.min', 'users', 'autosize.min'
	
.row
	.span2
		#filters
			=# current_user.company? if current_user.email == "ydiaz13@gmail.com"
			= render "users/filters"
	.span10
		#users
			= render @users
			.div{style:"clear: both"}
		= paginate @users, :remote => true