- content_for :javascript do
	= javascript_include_tag 'isotope.min', 'users', 'autosize.min'
	
.row
	.span2
		#filters
			= render "users/filters"
	.span10
		#users
			= render @users
			&nbsp;
		= paginate @users, :remote => true